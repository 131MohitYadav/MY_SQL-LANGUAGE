// HAVING CLAUSE


COMMAND -> SELECT DEPT_NO,COUNT(DEPT_NO) FROM EMP GROUP BY DEPT_NO HAVING COUNT(DEPT_NO) < 5;



CREATE TABLE DEPT(
DEPT_NO INT PRIMARY KEY,
D_NAME VARCHAR(40),
LOCATION VARCHAR(45)
);


INSERT INTO DEPT VALUES(
40,'IT','GUNA'
);

// FOR INNER JOIN
SELECT * FROM EMP INNER JOIN DEPT ON EMP.DEPT_NO = DEPT.DEPT_NO;